{
  "name": "bta3-al3ab-backend",
  "version": "1.0.0",
  "type": "module",
  "description": "Backend API for Bta3 Al3ab games management",
  "main": "server.js",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "postinstall": "npm install --no-audit --no-fund && npm --prefix ../frontend install --no-audit --no-fund && npm --prefix ../frontend run build",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "keywords": [
    "games",
    "api",
    "express"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.0",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongodb": "^7.0.0",
    "nodemailer": "^7.0.11"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}
